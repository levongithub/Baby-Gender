# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Baby Gender' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Baby Gender
    pod 'Firebase/Core'
    pod 'Firebase/AdMob'

  target 'Baby GenderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Baby GenderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
